# 🎬 LingoDrama

**AI-Powered Interactive Language Learning Theater**

An innovative English learning app that transforms vocabulary review into engaging short dramas. Each story is dynamically generated by Claude AI based on your personal vocabulary list.

---

## 🚀 Quick Start

### 1. Install Dependencies

```bash
npm install
```

### 2. Configure Claude API

1. Get your API key from [Anthropic Console](https://console.anthropic.com/)
2. Create `.env.local` file:

```bash
cp .env.local.example .env.local
```

3. Add your API key:

```env
ANTHROPIC_API_KEY=your-api-key-here
```

### 3. Run Development Server

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000)

---

## ✨ 核心特性

### 🏠 Dashboard（导演看板）
- 黑金主题配色，高端暗色模式
- 卡片式数据展示：总词汇量、今日需复习、掌握进度
- **红绿状态指示**：待复习单词卡片边框变红（警示），否则为绿（安全）
- 底部导航栏，单手操作友好

### 📥 Vault（词汇金库）
- **批量录入**：支持逗号/空格分隔，一次添加多个单词
- **AI 深度分析**：自动获取词性、难度、情感强度、标签
- **云端同步**：Supabase 数据库，跨设备访问
- 掌握度追踪（0-100%）

### 🎬 Review（AI Immersive Theater）
- **AI Story Generation**: Claude 3.5 Sonnet generates personalized dramas based on your words
- **Three Genres**: Romance/CEO, Workplace Comedy, Slice of Life
- **Smart Word Selection**: Automatically picks 3 words with lowest mastery level
- **Interactive Tooltips**: Hover over highlighted words for definitions (English or native language)
- **Chat-Style Format**: 8-10 messages with narration, dialogue, and plot twists
- **Fallback Stories**: Pre-written stories if API fails or no words available
- **Progress Tracking**: Message counter and restart functionality

### ⚡ Quiz（生存挑战）
最复杂的闯关测试模块，三种拟态 UI：

#### 💣 Mode A: Critical（生死时速）
- **适用**：动作词、紧急词
- **UI**：
  - 红色警报背景闪烁
  - CRT 扫描线特效
  - 倒计时大数字（10秒限时）
- **反馈**：
  - 答对 → "BOMB DEFUSED"（变绿）
  - 答错/超时 → "BOOM"（屏幕震动）

#### 💬 Mode B: Social（社交排雷）
- **适用**：情感词、抽象词
- **UI**：仿 Messenger/WeChat 聊天界面
- **反馈**：
  - 答对 → 对方发爱心 ❤️
  - 答错 → "Blocked"（被拉黑）

#### 📷 Mode C: Vibe（审美鉴定）
- **适用**：形容词、画面感强的词
- **UI**：仿 Instagram，图片初始高斯模糊
- **反馈**：
  - 答对 → 图片瞬间变清晰 + "Aesthetic Matched ✨"
  - 答错 → 图片变黑白 + "Wrong Vibe ❌"

---

## 🛠️ 技术栈

### 前端
- **Next.js 14** - App Router + TypeScript
- **Tailwind CSS** - 样式框架
- **Framer Motion** - 动画特效
- **Zustand** - 状态管理
- **Lucide React** - 图标库

### 后端 & 数据库
- **Supabase** - PostgreSQL + 实时订阅 + 认证
- **Row Level Security (RLS)** - 数据安全

### AI 服务
- **Claude API** - 单词分析 + 剧本生成
- **Unsplash API** - 背景图（开发环境）
- **Replicate AI** - 图片生成（生产环境）

---

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制示例文件：
```bash
cp .env.local.example .env.local
```

编辑 `.env.local`，填入你的 API 密钥：

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Claude API
CLAUDE_API_KEY=sk-ant-xxx

# Unsplash API（开发用）
UNSPLASH_ACCESS_KEY=your-key

# Replicate API（商用）
REPLICATE_API_TOKEN=r8_xxx
IMAGE_SERVICE=unsplash  # 或 replicate
```

### 3. 设置 Supabase

参考 [SUPABASE_SETUP.md](./SUPABASE_SETUP.md)：

1. 创建 Supabase 项目
2. 执行 `supabase-schema.sql` 创建数据库表
3. 复制 API 密钥到 `.env.local`

### 4. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000

---

## 📁 项目结构

```
lingodrama/
├── app/                    # Next.js App Router
│   ├── page.tsx           # Dashboard
│   ├── vault/page.tsx     # Vault 模块
│   ├── review/page.tsx    # Review 模块
│   ├── quiz/page.tsx      # Quiz 模块
│   └── globals.css        # 全局样式 + 特效
├── components/
│   ├── BottomNav.tsx      # 底部导航
│   └── quiz/              # Quiz 三种模式组件
│       ├── CriticalMode.tsx
│       ├── SocialMode.tsx
│       └── VibeMode.tsx
├── lib/
│   ├── types.ts           # TypeScript 类型定义
│   ├── store.ts           # Zustand 状态管理
│   ├── utils.ts           # 工具函数
│   └── supabase/          # Supabase 客户端
├── supabase-schema.sql    # 数据库 Schema
└── SUPABASE_SETUP.md      # Supabase 设置指南
```

---

## 🎨 设计特色

### 黑金主题配色
- 背景：`#0a0a0a`（纯黑）
- 卡片：`#121212`（深灰）
- 主色：`#d4af37`（金色）
- 成功：`#10b981`（绿）
- 危险：`#dc2626`（红）

### CSS 动画效果
```css
/* 震动 */
.shake { animation: shake 0.5s; }

/* 模糊淡入 */
.blur-in { animation: blur-in 0.6s; }

/* CRT 扫描线 */
.crt-scanlines { /* 老式显示器效果 */ }

/* 霓虹边框 */
.neon-border { box-shadow: 0 0 20px var(--primary); }
```

---

## 🔮 开发状态

### ✅ 已完成（MVP）
- ✅ 四大核心模块 UI/UX
- ✅ Mock 数据展示
- ✅ 动画特效系统
- ✅ 响应式布局

### 🔄 待集成（需 API 密钥）
- ⏳ Claude API（单词分析 + 剧本生成）
- ⏳ Supabase 数据库
- ⏳ Unsplash/Replicate 图片服务
- ⏳ 用户认证系统

### 🎯 未来功能
- [ ] 间隔重复算法（Spaced Repetition）
- [ ] 社交分享功能
- [ ] 每日打卡系统
- [ ] 多语言支持

---

## 📖 使用说明

### 添加单词
1. 进入 **Vault** 页面
2. 输入单词（逗号/空格分隔），如：`elastic, cynical, vibrant`
3. 点击"添加到金库"

### 复习单词
1. **Dashboard** 显示"今日需复习"数量
2. 点击卡片进入 **Review**
3. 阅读微剧本，单词在剧情中高亮
4. 点击"下一句"逐步推进

### 挑战测验
1. 进入 **Quiz** 页面
2. 根据单词类型自动进入不同模式：
   - 动作词 → Critical（倒计时）
   - 情感词 → Social（聊天）
   - 形容词 → Vibe（图片）
3. 必须全部答对才能打卡

---

## 📄 许可

MIT License
